import Types from "./types";
declare class LLMPrompts {
    private utils;
    constructor();
    forRealtimeSources(input: string, apiRepository: Array<Types.APIRepository>): {
        system: string;
        context: string;
        format: string;
        task: string;
    };
    forResponseWithData(input: string, data: any, agent: any): {
        system: string;
        context: string;
        format: string;
        task: string;
    };
}
export = LLMPrompts;
//# sourceMappingURL=llmprompts.d.ts.map